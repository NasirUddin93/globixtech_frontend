---
import TestMainLayout from "./TestMainLayout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";

const { title, description, publishDate, author, image } = Astro.props;
---

<TestMainLayout title={`${title} | Globix Tech Blog`}>
    <Navbar />
    <main class="pt-32 pb-24 bg-[#f5f5f7] min-h-screen">
        <div class="max-w-4xl mx-auto px-6">
            <!-- Article Header -->
            <div class="mb-12 text-center">
                <div class="flex items-center justify-center gap-3 text-xs font-bold text-[#86868b] mb-4">
                    <span class="bg-blue-50 text-[#0071e3] px-3.5 py-1 rounded-full border border-blue-100 uppercase tracking-widest text-[10px]">
                        Engineering Article
                    </span>
                    <span>&bull;</span>
                    <time class="flex items-center gap-1"><i class="far fa-calendar text-[#0071e3]"></i> {publishDate}</time>
                </div>
                <h1 class="text-3xl md:text-5xl font-extrabold text-[#1d1d1f] mb-6 leading-tight tracking-tight">
                    {title}
                </h1>
                {
                    description && (
                        <p class="text-lg md:text-xl text-[#86868b] mb-8 max-w-2xl mx-auto leading-relaxed font-medium">
                            {description}
                        </p>
                    )
                }
                <div class="flex items-center justify-center gap-3">
                    <div class="w-10 h-10 bg-blue-50 text-[#0071e3] rounded-full flex items-center justify-center font-bold text-sm">
                        <i class="fas fa-user"></i>
                    </div>
                    <div class="text-left">
                        <p class="font-bold text-[#1d1d1f] text-sm">
                            {author || "GlobixTech Team"}
                        </p>
                        <p class="text-xs text-[#86868b]">Software Engineer & Editor</p>
                    </div>
                </div>
            </div>

            <!-- Featured Image -->
            {
                image && (
                    <div class="rounded-[2.5rem] overflow-hidden shadow-[0_20px_60px_rgba(0,0,0,0.04)] mb-12 border border-white">
                        <img
                            src={image}
                            alt={title}
                            class="w-full h-auto object-cover max-h-[480px]"
                        />
                    </div>
                )
            }

            <!-- Content -->
            <article class="prose prose-lg prose-blue mx-auto bg-white/80 backdrop-blur-2xl p-8 md:p-14 rounded-[2.5rem] shadow-[0_10px_30px_rgba(0,0,0,0.02)] border border-white text-[#1d1d1f]">
                <slot />
            </article>

            <!-- Back Link -->
            <div class="mt-12 text-center">
                <a
                    href="/blog"
                    class="inline-flex items-center gap-2 text-[#0071e3] hover:text-[#0077ed] font-bold transition-all text-sm bg-white/80 backdrop-blur-md px-6 py-3 rounded-full border border-white shadow-xs"
                >
                    <i class="fas fa-arrow-left text-xs"></i> Back to All Articles
                </a>
            </div>
        </div>
    </main>

    <Footer />
</TestMainLayout>
